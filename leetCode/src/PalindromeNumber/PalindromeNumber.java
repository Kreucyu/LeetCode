package PalindromeNumber;

public class PalindromeNumber {
    public boolean isPalindrome(int x) {
        if(x < 11) return false;
        String y = String.valueOf(x);
        System.out.println(y.chars());
        return false;
    }
}
